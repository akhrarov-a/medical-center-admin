/**
 * <CreateDoctorForm /> props
 */
const useCreateDoctorFormProps = () => {
  // const router = useRouter();
  //
  // const { suppliers: store, products } = useStore();
  //
  // useEffect(() => {
  // }, []);

  return {
    // onSubmit: (values: SupplierForm) => store.createSupplier(values, router)
  };
};

export { useCreateDoctorFormProps };
